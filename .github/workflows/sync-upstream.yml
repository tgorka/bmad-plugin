name: Sync Upstream Sources

on:
  schedule:
    - cron: "0 0 * * 1" # Weekly Monday midnight UTC
  workflow_dispatch:

permissions:
  issues: write

jobs:
  check-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get upstream latest release
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/bmadcode/BMAD-METHOD/releases/latest --jq '.tag_name')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Latest upstream version: $VERSION"

      - name: Compare versions
        id: compare
        run: |
          CURRENT=$(jq -r '.version' .upstream-versions/core.json | sed 's/^v//')
          UPSTREAM=$(echo "${{ steps.upstream.outputs.version }}" | sed 's/^v//')
          echo "Current: $CURRENT"
          echo "Upstream: $UPSTREAM"
          if [ "$UPSTREAM" != "$CURRENT" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Already up to date."
          fi

      - name: Create issue if update needed
        if: steps.compare.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          UPSTREAM="${{ steps.upstream.outputs.version }}"
          CURRENT=$(jq -r '.version' .upstream-versions/core.json)

          EXISTING=$(gh issue list --repo ${{ github.repository }} --search "Upstream BMAD update: $UPSTREAM" --state open --json number -q '.[0].number')

          if [ -z "$EXISTING" ]; then
            gh label create upstream-sync --repo ${{ github.repository }} --description "Upstream version update" --color "0E8A16" 2>/dev/null || true

            gh issue create \
              --repo ${{ github.repository }} \
              --title "Upstream BMAD update: $UPSTREAM" \
              --body "A new version of [bmadcode/BMAD-METHOD](https://github.com/bmadcode/BMAD-METHOD) is available.

          ## Versions

          - **Current:** $CURRENT
          - **Upstream:** $UPSTREAM

          ## Action Required

          1. Update \`.upstream/BMAD-METHOD\` to the new version
          2. Diff changes against current plugin content
          3. Apply relevant updates to \`plugins/bmad/\`
          4. Update \`.upstream-versions/core.json\` to \`$UPSTREAM\`

          See [upstream releases](https://github.com/bmadcode/BMAD-METHOD/releases) for changelog." \
              --label "upstream-sync"
          fi

  check-tea:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get TEA latest release
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/bmad-code-org/bmad-method-test-architecture-enterprise/releases/latest --jq '.tag_name')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Latest TEA version: $VERSION"

      - name: Compare versions
        id: compare
        run: |
          CURRENT=$(jq -r '.version' .upstream-versions/tea.json | sed 's/^v//')
          UPSTREAM=$(echo "${{ steps.upstream.outputs.version }}" | sed 's/^v//')
          echo "Current: $CURRENT"
          echo "Upstream: $UPSTREAM"
          if [ "$UPSTREAM" != "$CURRENT" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Already up to date."
          fi

      - name: Create issue if update needed
        if: steps.compare.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          UPSTREAM="${{ steps.upstream.outputs.version }}"
          CURRENT=$(jq -r '.version' .upstream-versions/tea.json)

          EXISTING=$(gh issue list --repo ${{ github.repository }} --search "Upstream TEA update: $UPSTREAM" --state open --json number -q '.[0].number')

          if [ -z "$EXISTING" ]; then
            gh label create upstream-sync --repo ${{ github.repository }} --description "Upstream version update" --color "0E8A16" 2>/dev/null || true

            gh issue create \
              --repo ${{ github.repository }} \
              --title "Upstream TEA update: $UPSTREAM" \
              --body "A new version of [bmad-code-org/bmad-method-test-architecture-enterprise](https://github.com/bmad-code-org/bmad-method-test-architecture-enterprise) is available.

          ## Versions

          - **Current:** $CURRENT
          - **Upstream:** $UPSTREAM

          ## Action Required

          1. Update \`.upstream/bmad-method-test-architecture-enterprise\` to the new version
          2. Run \`bun run sync\` to update plugin content
          3. Run \`bun run validate\` to verify coverage
          4. Version file \`.upstream-versions/tea.json\` is updated automatically by sync

          See [upstream releases](https://github.com/bmad-code-org/bmad-method-test-architecture-enterprise/releases) for changelog." \
              --label "upstream-sync"
          fi

  check-bmb:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get BMB latest release
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/bmad-code-org/bmad-builder/releases/latest --jq '.tag_name')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Latest BMB version: $VERSION"

      - name: Compare versions
        id: compare
        run: |
          CURRENT=$(jq -r '.version' .upstream-versions/bmb.json | sed 's/^v//')
          UPSTREAM=$(echo "${{ steps.upstream.outputs.version }}" | sed 's/^v//')
          echo "Current: $CURRENT"
          echo "Upstream: $UPSTREAM"
          if [ "$UPSTREAM" != "$CURRENT" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Already up to date."
          fi

      - name: Create issue if update needed
        if: steps.compare.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          UPSTREAM="${{ steps.upstream.outputs.version }}"
          CURRENT=$(jq -r '.version' .upstream-versions/bmb.json)

          EXISTING=$(gh issue list --repo ${{ github.repository }} --search "Upstream BMB update: $UPSTREAM" --state open --json number -q '.[0].number')

          if [ -z "$EXISTING" ]; then
            gh label create upstream-sync --repo ${{ github.repository }} --description "Upstream version update" --color "0E8A16" 2>/dev/null || true

            gh issue create \
              --repo ${{ github.repository }} \
              --title "Upstream BMB update: $UPSTREAM" \
              --body "A new version of [bmad-code-org/bmad-builder](https://github.com/bmad-code-org/bmad-builder) is available.

          ## Versions

          - **Current:** $CURRENT
          - **Upstream:** $UPSTREAM

          ## Action Required

          1. Update \`.upstream/bmad-builder\` to the new version
          2. Run \`bun run sync\` to update plugin content
          3. Run \`bun run validate\` to verify coverage
          4. Version file \`.upstream-versions/bmb.json\` is updated automatically by sync

          See [upstream releases](https://github.com/bmad-code-org/bmad-builder/releases) for changelog." \
              --label "upstream-sync"
          fi

  check-cis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get CIS latest release
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/bmad-code-org/bmad-module-creative-intelligence-suite/releases/latest --jq '.tag_name')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Latest CIS version: $VERSION"

      - name: Compare versions
        id: compare
        run: |
          CURRENT=$(jq -r '.version' .upstream-versions/cis.json | sed 's/^v//')
          UPSTREAM=$(echo "${{ steps.upstream.outputs.version }}" | sed 's/^v//')
          echo "Current: $CURRENT"
          echo "Upstream: $UPSTREAM"
          if [ "$UPSTREAM" != "$CURRENT" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Already up to date."
          fi

      - name: Create issue if update needed
        if: steps.compare.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          UPSTREAM="${{ steps.upstream.outputs.version }}"
          CURRENT=$(jq -r '.version' .upstream-versions/cis.json)

          EXISTING=$(gh issue list --repo ${{ github.repository }} --search "Upstream CIS update: $UPSTREAM" --state open --json number -q '.[0].number')

          if [ -z "$EXISTING" ]; then
            gh label create upstream-sync --repo ${{ github.repository }} --description "Upstream version update" --color "0E8A16" 2>/dev/null || true

            gh issue create \
              --repo ${{ github.repository }} \
              --title "Upstream CIS update: $UPSTREAM" \
              --body "A new version of [bmad-code-org/bmad-module-creative-intelligence-suite](https://github.com/bmad-code-org/bmad-module-creative-intelligence-suite) is available.

          ## Versions

          - **Current:** $CURRENT
          - **Upstream:** $UPSTREAM

          ## Action Required

          1. Update \`.upstream/bmad-module-creative-intelligence-suite\` to the new version
          2. Run \`bun run sync\` to update plugin content
          3. Run \`bun run validate\` to verify coverage
          4. Version file \`.upstream-versions/cis.json\` is updated automatically by sync

          See [upstream releases](https://github.com/bmad-code-org/bmad-module-creative-intelligence-suite/releases) for changelog." \
              --label "upstream-sync"
          fi

  check-gds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get GDS latest release
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(gh api repos/bmad-code-org/bmad-module-game-dev-studio/releases/latest --jq '.tag_name')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Latest GDS version: $VERSION"

      - name: Compare versions
        id: compare
        run: |
          CURRENT=$(jq -r '.version' .upstream-versions/gds.json | sed 's/^v//')
          UPSTREAM=$(echo "${{ steps.upstream.outputs.version }}" | sed 's/^v//')
          echo "Current: $CURRENT"
          echo "Upstream: $UPSTREAM"
          if [ "$UPSTREAM" != "$CURRENT" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Already up to date."
          fi

      - name: Create issue if update needed
        if: steps.compare.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          UPSTREAM="${{ steps.upstream.outputs.version }}"
          CURRENT=$(jq -r '.version' .upstream-versions/gds.json)

          EXISTING=$(gh issue list --repo ${{ github.repository }} --search "Upstream GDS update: $UPSTREAM" --state open --json number -q '.[0].number')

          if [ -z "$EXISTING" ]; then
            gh label create upstream-sync --repo ${{ github.repository }} --description "Upstream version update" --color "0E8A16" 2>/dev/null || true

            gh issue create \
              --repo ${{ github.repository }} \
              --title "Upstream GDS update: $UPSTREAM" \
              --body "A new version of [bmad-code-org/bmad-module-game-dev-studio](https://github.com/bmad-code-org/bmad-module-game-dev-studio) is available.

          ## Versions

          - **Current:** $CURRENT
          - **Upstream:** $UPSTREAM

          ## Action Required

          1. Update \`.upstream/bmad-module-game-dev-studio\` to the new version
          2. Run \`bun run sync\` to update plugin content
          3. Run \`bun run validate\` to verify coverage
          4. Version file \`.upstream-versions/gds.json\` is updated automatically by sync

          See [upstream releases](https://github.com/bmad-code-org/bmad-module-game-dev-studio/releases) for changelog." \
              --label "upstream-sync"
          fi
